namespace Aldsoft.Acord.LA
{
    using System.Collections.Generic;
    using System;

    public partial class Relation_Type
    {
        public void ValidateRelation()
        {
            //var relation = Relation.AllowedRelationships[this.RelationRoleCode.tc];
            //if (string.IsNullOrWhiteSpace(this.RelatedObjectID) && !string.IsNullOrWhiteSpace(this.OriginatingObjectID))
            //    return; // This is okay, they are likely relating an estate to a carrier/holding.

            //if (relation.RelatedObjectType != this.RelatedObjectType.tc)
            //    throw new InvalidOperationException("Related objects don't match");

            //if (relation.OriginatingObjectType != this.OriginatingObjectType.tc)
            //    throw new InvalidOperationException("Originating objects don't match");
        }
    }
}
